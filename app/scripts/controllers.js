'use strict';

/* Controllers */

angular.module('controllers', [])
    .controller('oneController', ['$rootScope', function ($rootScope) {
    }])

    .controller('anotherController', ['$scope', function ($scope) {
    }]);
